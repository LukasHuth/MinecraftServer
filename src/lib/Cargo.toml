[package]
name  = "packet_api"
version = "0.0.1"

[lib]
name = "packet_api"
path = "lib.rs"

[dependencies]
rand = { version = "0.8.5", features = ["small_rng"] }
regex = "1.10.3"
serde = { version = "1.0.197", features = ["derive"] }
serde_bytes = "0.11.14"
serde_json = "1.0.114"
